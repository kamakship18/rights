# Prisma — Local Docker (default for dev)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/justice_os?schema=public"

# When migrating to Neon (Phase C), replace with:
# DATABASE_URL="postgresql://<user>:<password>@<host>/<dbname>?sslmode=require"

# ── Demo Seed Config ─────────────────────────────────
# Officer email (all 3 demo officers use this so you see demo emails)
DEMO_OFFICER_EMAIL="team@actionablejustice.dev"

# Twilio-verified phone numbers for emergency contacts (SOS demo)
DEMO_PHONE_1="+919999999991"
DEMO_PHONE_2="+919999999992"
